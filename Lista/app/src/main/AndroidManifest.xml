<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.paul.lista">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Deadpool"
            android:label="@string/label_deadpool"/>
        <activity
            android:name=".FilmsActivity"
            android:label="@string/label_films"/>
        <activity
            android:name=".PaintedMan"
            android:label="@string/label_painted_man" />
        <activity
            android:name=".BooksActivity"
            android:label="@string/label_books" />
        <activity
            android:name=".AppInfoActivity"
            android:label="@string/label_app_info" />
        <activity android:name=".TopLvlActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>